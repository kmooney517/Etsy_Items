var price = items.price;

console.log(price);

function avgPrice(chcolate) {
  var sum = chocolate.reduce(function(prevValue, currValue) {
    return (prevValue + currValue);
  });
  var average = sum / price.length;
    return average;
};




